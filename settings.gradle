pluginManagement {
    repositories {
        mavenCentral()
        maven {
            url 'https://artifactory.global.dish.com/artifactory/repo'
        }
    }
}

rootProject.name = 'kafka-project'
